/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "hxl.550.myftpupload.com",
            "smartplay-content.payloadcms.app",
        ],
    },
};

export default nextConfig;
